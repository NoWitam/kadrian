# D07 — Initial output is H.264 MP4 in 720p and 1080p

- Status: Accepted
- Recorded: 2026-09-21, from `AGENTS.md` at commit `250ae8f`
- Related: D06

## Decision

> D07: Initial output is H.264 MP4 in 720p and 1080p.

## Rationale

Not recorded. `AGENTS.md` states this decision without its reasons. The project
owner is asked to supply the rationale; until then this section stays empty
rather than guessed.

## Related rules in `AGENTS.md`

Verbatim quotations. `AGENTS.md` remains the authoritative text.

> The Producer exports an H.264 MP4 without storing all intermediate frames.

> Use one 10-second, 1080x1920, 30 fps composition containing:

Listed under "Explicitly outside the current scope":

> CRDT collaboration, public plugins, 4K, HDR, and desktop/mobile apps.

## See also

- [Vertical-spike specification](../spike/vertical-spike.md), open question Q2:
  the reference composition is portrait, so the pixel dimensions behind "720p"
  and "1080p" and the way the smaller preset is produced are still open.

## Verification

Not mechanically verified in PR-00. The spike verifies the exported file's
container, codec, pixel format, dimensions, frame rate, and frame count with
`ffprobe`.
