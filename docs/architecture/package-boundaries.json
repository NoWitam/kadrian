{
  "$comment": "Normative package boundary map, enforced by tests/repo/package-boundaries.test.ts. Read docs/adr/D12-package-dependency-map.md before editing: removing a prohibition requires an ADR.",
  "packages": {
    "schema": {
      "allowedDependencies": []
    },
    "runtime": {
      "allowedDependencies": ["schema"]
    },
    "editor-sdk": {
      "allowedDependencies": ["schema"]
    },
    "ai-sdk": {
      "allowedDependencies": ["editor-sdk", "schema"]
    },
    "renderer-dom": {
      "allowedDependencies": ["runtime", "schema"]
    },
    "player": {
      "allowedDependencies": ["renderer-dom", "runtime", "schema"]
    },
    "producer": {
      "allowedDependencies": ["renderer-dom", "runtime", "schema"]
    },
    "cli": {
      "allowedDependencies": ["producer", "schema"]
    },
    "test-fixtures": {
      "allowedDependencies": [],
      "devOnly": true
    }
  },
  "prohibited": [
    {
      "from": ["schema"],
      "to": ["*"],
      "reason": "The schema package is the persistent data contract and the root of the dependency graph."
    },
    {
      "from": ["test-fixtures"],
      "to": ["*"],
      "reason": "Fixtures are pure data. Every package may devDepend on them, so any outgoing edge would create a cycle."
    },
    {
      "from": ["editor-sdk", "ai-sdk"],
      "to": ["runtime", "renderer-dom", "player", "producer"],
      "reason": "Commands are document transforms. UI and AI share one command bus that must work without a renderer."
    },
    {
      "from": ["producer"],
      "to": ["player"],
      "reason": "Player and Producer share the runtime build. Shared code moves down into renderer-dom or runtime, never sideways."
    },
    {
      "from": ["player"],
      "to": ["producer"],
      "reason": "Player and Producer share the runtime build. Shared code moves down into renderer-dom or runtime, never sideways."
    },
    {
      "from": ["runtime"],
      "to": ["renderer-dom"],
      "reason": "The evaluation stays free of any environment; rendering builds on it, never the reverse (D20)."
    },
    {
      "from": ["renderer-dom"],
      "to": ["player", "producer"],
      "reason": "Both hosts load the renderer as part of the one runtime build, so it cannot know either host (D20)."
    }
  ],
  "externalRuntimeDependencies": {
    "playwright-core": {
      "reason": "The Producer drives the pinned Chromium through Playwright; the exact version pins the browser build (D26.1). Used by @kadrion/producer only.",
      "license": "Apache-2.0"
    }
  }
}
